pipeline {
  agent any
  stages {

    stage ( 'BUILD' ) {
      steps {
        echo "This is build stage"
        sh '''
                sleep 5
                exit 0
           '''
        
      }
    }
  }
  
}
